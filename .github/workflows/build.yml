name: Build Android
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # v4 para evitar errores deprecated
      
      # FIX: Instalar Java 17 (Requerido por Android moderno)
      - uses: actions/setup-java@v4 # v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0' # Versión estable recomendada

      - run: flutter pub get

      # FIX CRÍTICO: Arreglar saltos de línea de Windows y dar permisos
      - run: |
          sed -i 's/\r$//' android/gradlew
          chmod +x android/gradlew
      
      - run: flutter build apk --release
      
      - uses: actions/upload-artifact@v4 # v4 para evitar errores deprecated
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk